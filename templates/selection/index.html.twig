{# set current menu #}
{% set activeMenu = 'center' %}
{% set subMenu = 'center' %}
{% set activeMenu2 = 'selection' %}

{% extends 'layout/app.html.twig' %}

{% block body %}
    <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="{{ path('projects.index') }}" title="{{ 'app.home'|trans }}">{{ 'app.home'|trans }}</a></li>
            <li class="breadcrumb-item"><a href="{{ path('project.user.list', { 'id' : project.id }) }}"
                                           title="{{ project.acronyme }}">{{ project.acronyme }}</a></li>
            <li class="breadcrumb-item active">{{ 'entity.Center.action.list'|trans }}</li>
        </ol>
    </nav>

    <h1>{{ 'entity.Center.action.list'|trans }} <br>
        {% if is_granted('PROJECT_ACCESS_AND_OPEN', project) and is_granted('CENTER_CREATE') %}
            <a class="btn btn-primary" href="{{ path('project.center.new', {'id' : project.id}) }}">{{ 'entity.Center.action.new'|trans }}</a>
        {% endif %}
    </h1>

    {{ list.render|raw }}

{% endblock %}
