{% set activetab = activetab is defined ? activetab : '' %}

<ul class="nav nav-tabs">

    <li class="nav-item">

        {% if deviation is not null %}
            <a class="nav-link {{ 'declaration' == activetab ? ' active' : ''}}" href="{{ path('deviation.declaration', { 'projectID' : project.id, 'deviationID': deviation.id, 'edit': editMode }) }}">{{ 'deviation.titre.declaration'|trans|capitalize }}</a>
        {% else %}
            <a class="nav-link {{ 'declaration' == activetab ? ' active' : ''}}" href="{{ path('deviation.declaration', { 'projectID' : project.id }) }}">{{ 'deviation.titre.declaration'|trans|capitalize }}</a>
        {% endif %}

    </li>

    <li class="nav-item">

        {% if deviation is not null %}
            <a class="nav-link {{ 'review' == activetab ? ' active' : ''}}" href="{{ path('deviation.review', { 'projectID' : project.id, 'deviationID' : deviation.id }) }}">{{ 'deviation.titre.review'|trans|capitalize }}</a>
        {% else %}
            <a class="nav-link {{ 'review' == activetab ? ' active' : ''}}" href="{{ path('deviation.review', { 'projectID' : project.id }) }}">{{ 'deviation.titre.review'|trans|capitalize }}</a>
        {% endif %}

    </li>

    <li class="nav-item">

        {% if deviation is not null %}
            <a class="nav-link {{ 'sample' == activetab ? ' active' : ''}}" href="{{ path('deviation.sample', { 'projectID' : project.id, 'deviationID': deviation.id }) }}">{{ 'deviation.titre.associateSample'|trans|capitalize }}</a>
        {% else %}
            <a class="nav-link {{ 'sample' == activetab ? ' active' : ''}}" href="{{ path('deviation.sample', { 'projectID' : project.id }) }}">{{ 'deviation.titre.associateSample'|trans|capitalize }}</a>
        {% endif %}

    </li>

</ul>
