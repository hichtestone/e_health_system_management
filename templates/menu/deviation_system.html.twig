{% set activetab = activetab is defined ? activetab : '' %}

<ul class="nav nav-tabs">

    <li class="nav-item">

        {% if deviationSystem is not null %}
            <a class="nav-link {{ 'declaration' == activetab ? ' active' : ''}}" href="{{ path('deviation.system.declaration', { 'deviationSystemID': deviationSystem.id, 'edit': editMode }) }}">{{ 'deviation.titre.declaration'|trans|capitalize }}</a>
        {% else %}
            <a class="nav-link {{ 'declaration' == activetab ? ' active' : ''}}" href="{{ path('deviation.system.declaration') }}">{{ 'deviation.titre.declaration'|trans|capitalize }}</a>
        {% endif %}

    </li>

    <li class="nav-item">

        {% if deviationSystem is not null %}
            <a class="nav-link {{ 'reviewCREX' == activetab ? ' active' : ''}}" href="{{ path('deviation.system.review.list', { 'deviationSystemID' : deviationSystem.id }) }}">Revues CREX</a>
        {% else %}
            <a class="nav-link {{ 'reviewCREX' == activetab ? ' active' : ''}}" href="{{ path('deviation.system.review.list') }}">{{ 'deviation.titre.review'|trans|capitalize }}</a>
        {% endif %}

    </li>

</ul>
